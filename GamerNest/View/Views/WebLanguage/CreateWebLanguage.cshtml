@model Support.WebLanguageModel

@{
    ViewData[ "Title" ] = "Admin - Create Web Languages";
}
<main class="home-section">
    <div class="text">
        <h1>Languages for the Web</h1>
        <h2>@ViewData[ "TitleCreate" ]</h2>

        <section class="Createform">
            <form asp-controller="WebLanguage" asp-action="Create" method="post" enctype="multipart/form-data">
                <partial name="_Error" />

                <div>
                    <div>
                        <label asp-for="name">@ViewData[ "fId" ]:</label>
                        <input asp-for="id" placeholder="@ViewData[ "fId" ]" required />
                    </div>
                    <div>
                        <label asp-for="name" class="required">@ViewData[ "fName" ]:</label>
                        <input required asp-for="name" placeholder="@ViewData[ "fName" ]" />
                    </div>
                    <div>
                        <label asp-for="icon" class="required">@ViewData[ "fIcon" ]:</label>
                        <input required asp-for="icon" placeholder="@ViewData[ "fIcon" ]" />
                    </div>
                </div>

                <div>
                    <div>
                        <button type="submit" class="mainButton">@ViewData[ "ButtonCreate" ]</button>
                    </div>

                </div>

            </form>
        </section>


    </div>
</main>